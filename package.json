{
  "name": "ai-discovery-platform",
  "version": "1.0.0",
  "description": "AI-Powered Content Discovery Platform with RAG Pipeline",
  "private": true,
  "workspaces": ["apps/backend", "apps/frontend"],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm start --workspaces"
  }
}
